import axiosClient from "../lib/axiosClient";

// TODO: Cập nhật endpoint đúng khi có từ backend

// Placeholder - trả về mảng rỗng để trang không bị crash
export const getAllBorrowings = async (status = "") => {
  // TODO: Thay bằng endpoint đúng
  // const params = {};
  // if (status) params.status = status;
  // const response = await axiosClient.get("/api/borrowings/...", { params });
  // return response.data;
  
  return { result: [] };
};
